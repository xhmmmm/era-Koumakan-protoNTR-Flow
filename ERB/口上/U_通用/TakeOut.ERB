;----------------------------------------------------------------------------------------------------
;和访问者去访问者家的口上
;----------------------------------------------------------------------------------------------------
;用来代替原版中和“苦主同意出轨”的内容过度耦合的部分，毕竟那个内容也已经被移除了，所以有必要做一些新的。

;和访问者去他家经过玩家身边
@NTR_ROOM_KU_GOTO_SEEN(角色)
#DIM 角色

;和访问者去他家没有经过玩家身边
@NTR_ROOM_KU_GOTO(角色)
#DIM 角色
;在主动去访问者家的情况下，有以下口上配置
;FUNC NTR_KOJO_K{CFLAG:角色:口上パターン}_10_0(角色)
;FUNC NTR_KOJO_K_10_0(角色)
;虽然也可以直接拿过来，但是我觉得可以做个不一样的

;和访问者去他家进入他家之后
@NTR_ROOM_KU_GOTO_AFTER(角色)
#DIM 角色

;----------------------------------------------------------------------------------------------------
;
;----------------------------------------------------------------------------------------------------