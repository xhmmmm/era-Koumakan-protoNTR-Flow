;=================================================
; 跨领域移动条件判定
;
; 同领域内：自由移动，无限制
; 跨领域：  需要条件判定（时间、剧情等）
;           NPC 默认不允许自由跨领域
;           玩家 需满足条件（如剩余时间充足）
;
; 当前所有位置同属 领域_红魔馆及周边，因此此函数目前始终返回1。
; 新增领域时在此添加条件逻辑。
;=================================================

;=================================================
; CAN_CROSS_REALM — 判断角色是否可以跨越领域
;
; @param 角色     角色编号
; @param 从领域   出发领域编号
; @param 到领域   目标领域编号
; @return 1=允许跨领域, 0=不允许
;=================================================
@CAN_CROSS_REALM(角色, 从领域, 到领域)
#FUNCTION
#DIM 角色
#DIM 从领域
#DIM 到领域

; 同领域直接放行
SIF 从领域 == 到领域
	RETURNF 1

; --- NPC 跨领域限制 ---
; NPC 默认不允许自由跨领域，除非剧情设置了许可标志
IF 角色 != MASTER
	; CFLAG:角色:跨领域许可 可由剧情事件设置
	SIF !CFLAG:角色:跨领域许可
		RETURNF 0
ENDIF

; --- 玩家跨领域条件 ---
; 未来扩展示例：
; IF 从领域 == 领域_红魔馆及周边 && 到领域 == 领域_人间之里周边
;     ; 需要至少4小时（240分钟）的剩余时间
;     SIF TIME > 1200
;         RETURNF 0
;     RETURNF 1
; ENDIF

; 默认：当前只有一个领域，不会走到这里
RETURNF 1
